---
"@adaline/anthropic": patch
---

Add current-generation Voyage embedding models: `voyage-3.5`, `voyage-3.5-lite`, `voyage-3-large`, and `voyage-code-3`. All support up to 32K input tokens and a default output dimension of 1024 (configurable up to 2048 via the embeddings request).
